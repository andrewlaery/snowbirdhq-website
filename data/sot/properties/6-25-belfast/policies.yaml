# yaml-language-server: $schema=../_schemas/policies.json
schema_version: 1
slug: 6-25-belfast
access_codes:
  front_door_master: ''  # TODO: lockbox code (kept internal)
  garage: ''
  alarm: ''  # building alarm — disarm by re-swiping FOB if triggered
par_levels: []  # TODO: backfill from cleaning audit (item locations
                # register exists at docs/property-monitoring/)
replacement_cycles:
  - item: pillows
    cycle_months: 12
  - item: linens
    cycle_months: 24
  - item: towels
    cycle_months: 12
  - item: small_appliances
    cycle_months: 48
  - item: mattress
    cycle_months: 96
owner_setpoints:
  heating:
    winter_min_temp_c: 18.0  # main floor target (Pete-recommended baseline)
    summer_max_temp_c: 10.0  # cool-down setpoint when wanted
cleaning_standards_url: ''
known_issues:
  - description: >
      Front-door intercom system does not work — there is no doorbell
      from the building gate, and remote access from the apartment
      doesn't function.
    severity: minor
  - description: >
      Building alarm can trigger if the FOB is swiped multiple times in
      quick succession at the front door — re-swipe to disarm.
    severity: minor
