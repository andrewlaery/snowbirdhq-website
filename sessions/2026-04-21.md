# Session — 2026-04-21

Short.io → self-hosted short-link redirector. Plus research on property-specific data for the 9 placeholder compendiums (documented but not yet executed).

## Topics
- Research pass across the workspace to inventory property-specific data for 9 placeholder compendiums (10b-delamare, 1-34-shotover, 2-34-shotover, 3-15-gorge, 10-15-gorge, 14-15-gorge, 6a-643-frankton, 73a-hensman, 73b-hensman, 100-risinghurst-home, 100-risinghurst-unit, 41-suburb-bonsai). Parallel researcher agents covered property registries/OTA, operational/IoT/guest-ops data, and narrative/vault sources. Findings in the conversation — migration tiers ready but not actioned this session.
- Short-link migration decision: keep Short.io or self-host. User chose self-host on existing Vercel (zero incremental cost since Pro plan already includes this).
- Short-link migration execution end-to-end: inventory (14 existing Short.io links), route handler, typed map, host-conditional rewrite, Vercel domain attach, DNS cutover via Cloudflare dashboard (using Claude-in-Chrome), live HTTPS verification.
- Phase 0 decisions: `41-suburb-bonsai` → rename to `41-suburb-basecamp` (registry/OTA/HA all use BaseCamp, Bonsai listing is dead). `14-15-gorge` → real Whistler unit, inherit from `3-15-gorge` template at compendium time.

## Findings

### Short.io inventory (2026-04-21)
14 active links on `go.bcampx.com`:
- 3 `-010-LandingPage` (7-Suburb, 25-Dublin, 6-25-Belfast) — already pointing at new docs
- 9 deep-page links (HouseRules / UserInstructions / CriticalAndEssentialInformation variants) — all still pointing at obsius.site (set to decommission ~2026-05-20)
- 1 `SnowbirdHQ-QueenstownInsights` — also still pointing at Obsius (sync script updated MDX but never updated the short link)
- 1 utility: `34-Shotover-Car-Park-Map` → Google Drive PDF
Migration was opportunity to pre-empt the Obsius decommission for all the deep-page links too.

Data-quality quirks in the Short.io register:
- `6-25-Belfast-011-CriticalAndEssentialInformation` — misnumbered `011` (should be `013`). Preserved the path as-is.
- Mixed casing across property paths (`10B-DeLaMare`, `6-25-Belfast`, etc.) — preserved exactly to avoid breaking printed materials.

### Architecture decisions

**Self-host on existing Vercel vs bcampx Docker vs Shlink vs keep Short.io**: chose Vercel because (a) already on Pro plan so incremental cost is zero, (b) destinations live in git (reviewable, reversible, no dashboard), (c) no API keys to rotate, (d) "if the website is up, short links are up" — one fewer thing to monitor.

**Three-kind redirect schema** (`property` / `path` / `external`):
- `property` handles the bulk (slug + optional page, auto-appends `?access=<DOCS_ACCESS_KEY>`)
- `path` covers shared pages like Queenstown Insights without special-casing
- `external` covers the one non-docs link (34 Shotover car-park Google Drive)
- Single schema, simple lookup, typed const map. No DB, no dashboard.

**Route implementation**: Edge runtime, GET only, unknown slug → 404 (not a default landing — deliberate, no path-enumeration signal). Access key read from env at request time so rotation doesn't require touching the map.

### DNS + TLS cutover

- `bcampx.com` is on Cloudflare nameservers (NOT Vercel DNS, which is what `snowbirdhq.com` uses — mental model correction for the project).
- Used Claude-in-Chrome browser automation to edit the Cloudflare DNS record — Type CNAME → A, Target cname.short.io → 76.76.21.21, TTL 1hr → 1min for cutover safety.
- DNS propagated to all major resolvers (1.1.1.1, 8.8.8.8) within ~2 minutes. Very fast — Cloudflare's default TTL + anycast.
- Vercel TLS cert (Let's Encrypt) took ~2–3 minutes after DNS pointed at Vercel. During that gap, HTTPS failed with `SSL_ERROR_SYSCALL` but HTTP-port-80 already worked correctly — proof the rewrite was live even before HTTPS was usable.
- Full end-to-end verification over live `https://go.bcampx.com`: all 10 sampled paths returned correct 302s, unknown slug returned 404, external (Google Drive) redirect worked, full browser flow (short link → docs → cookie set) passed.

### Vercel domain attachment quirks

- `vercel domains add go.bcampx.com` (single arg, NOT `<domain> <project>` like the help suggests) — took a couple of attempts to get the CLI invocation right.
- After attach, `vercel domains inspect go.bcampx.com` warns about nameservers not matching Vercel's. This is noise when using the A-record approach — Vercel still provisions TLS and serves traffic correctly without owning the whole zone. Safe to ignore.

### Property compendium research — NOT executed this session

Produced a 4-tier migration plan + data-availability matrix but did not author any compendiums. Blockers resolved (bonsai→basecamp rename, 14-15-gorge inheritance decision). Authoring happens in subsequent sessions, one property per session roughly. 10b-delamare identified as the richest-data starting point (250-line guest-ops KB, admin cheat sheet, 6 photos).

## Next Steps
- **2026-04-23**: execute Phase 5 (Short.io decommission) — delete 14 links, cancel subscription, revoke API key `sk_S8pzg...`. 48h soak window ends then.
- Kick off compendium authoring per the tiered plan — start with `10b-delamare` (richest data).
- Rename `41-suburb-bonsai` → `41-suburb-basecamp` across `src/data/properties.ts`, `public/properties/`, `content/docs/properties/`, `meta.json` before the first BaseCamp compendium authoring session.
- Investigate Vercel ↔ GitHub auto-deploy reliability — several pushes today and yesterday required manual `vercel --prod`. Backlog item.

---

# Session — 2026-04-21 (pm — compendium authoring batch + branded short domain)

Second sitting of the day. Authored 12 new property compendiums, added 30 short-form aliases, executed the 41-suburb-bonsai → basecamp rename, and added `go.snowbirdhq.com` as a second branded short-link domain.

## Topics
- Short-form path aliases: 30 new entries (canonical PascalCase + lowercase per property) — no route changes, just more map entries.
- Compendium authoring: property-by-property, in decreasing data-richness order. 10b-delamare, 1-34-shotover, 2-34-shotover, 6a-643-frankton, 73b-hensman, 3-15-gorge, 10-15-gorge, 14-15-gorge, 41-suburb-basecamp (with rename), 100-risinghurst-home, 100-risinghurst-unit, 73a-hensman.
- 41-suburb-bonsai → 41-suburb-basecamp rename across src/data/properties.ts, content/docs/properties/, public/properties/, meta.json. hostawayId 428528 → 500890.
- go.snowbirdhq.com branded domain attached alongside go.bcampx.com.

## Findings

### Authoring patterns established
- **Vault-first authoring**: four `.md` files per property in `~/Documents/andrewlaery/SnowbirdHQ/Property/{Name}/` following the `1.0/1.1/1.2/1.3_{Name}_ *.md` naming. Sync script regenerates MDX in the repo.
- **Public-guide scrubs from the internal KB**:
  - WiFi password → "provided on the welcome card at check-in" (strict)
  - Lockbox code → "provided in your booking information" (matches 7-Suburb convention)
  - Owner names / phone numbers / trust names / bank accounts → excluded entirely
  - Internal "known issues" sections → excluded (they're AI context for the guest-ops agent, not guest-facing)
  - Internal response-guidance notes (e.g. "never minimise noise complaints") → translated into upfront public disclosure of the noise source itself, so guests arrive prepared rather than surprised.
- **Source precedence when KBs conflict**: guest-ops KB > admin cheat sheet > Hostaway listing. 34 Shotover cheat sheet said "Vogue 2-in-1 washer dryer", guest-ops said "AEG washing machine (new Mar 2026)" — used AEG. Similar "NOKIA-6791" vs "TP-Link_F8D6" WiFi discrepancy on 10B De La Mare — used the current guest-ops KB value.

### Known data gaps worth capturing
- **73a-hensman**: WiFi password has a flagged typo (`Lakeviewipper123` vs likely `Lakeviewupper123`). Owner verification needed. Fuse box + mains water shutoff + heating specifics + kitchen + laundry all TODO. The compendium ships with a visible "some details being finalised — message us" note in User Instructions.
- **41-suburb-basecamp**: Smart lock confirmed (code sent 2 days before arrival), WiFi SSID "BaseCamp", off-street parking. Beyond that the KB is sparse. Compendium is thinner than the others.
- **14-15-gorge**: Authored by template-copying the 10-15-gorge compendium and substituting unit references. No unit-specific KB content beyond "street parking only, lockbox" — inherited Whistler Apartments shared features (privately managed, reception not affiliated, 03 409 0394 building-manager number).
- **Photo gaps** (marketing gallery pages affected, docs portal not affected): 73a-hensman, 14-15-gorge, 100-risinghurst-home, 100-risinghurst-unit.

### Technical gotchas hit this session
- **Bash sed/cat on ~/Documents/** fails with "Operation not permitted" even with `dangerouslyDisableSandbox: true` — macOS Documents-folder TCC protection sits above the sandbox. Write tool works (presumably via a different syscall path). Workaround: skip the Bash file-copy-with-substitutions approach and just Write each file directly. Cost ~5 min to realise.
- **Parallel Write + Bash race**: calling `./scripts/sync-property-compendium.sh --only <slug>` in the same message as the Write calls for that vault's files runs BEFORE the Writes finish. Result: sync finds zero files, returns "Copied 0". Fix: call sync sequentially AFTER the Writes in a separate tool_use round.
- **Pre-commit trailing-whitespace / EOF hooks are noisy** on re-synced MDX — every `.mdx` file that changed gets auto-fixed on first commit attempt, commit aborts, second try succeeds. Only cosmetic but worth knowing.

### Short-link architecture observations confirmed
- `go.snowbirdhq.com` attach was significantly faster than `go.bcampx.com` because `snowbirdhq.com` is on Vercel nameservers (yesterday's addition to `go.bcampx.com` required a Cloudflare DNS change). Vercel auto-provisioned the A record and TLS cert within 2-3 minutes of `vercel domains add`.
- Short-link lookup is case-sensitive on path (confirmed via curl against unknown-but-differently-cased paths returning 404). The map has explicit entries for both casings per property, which is why `/7-Suburb` and `/7-suburb` both work but `/7-SUBURB` would 404.

## Next Steps
- **2026-04-23 (Thursday)**: Phase 5 Short.io decommission. Delete 14 links, cancel subscription, revoke API key `sk_S8pzg...`.
- **Per-page content review** via `docs/CONTENT_REVIEW.md`. Recommend starting with the thinner compendiums (73a-hensman, 14-15-gorge, 41-suburb-basecamp) where gaps are most likely to surface.
- **Owner phone call** to fill 73a-hensman data gaps — then re-author + sync.
- **Photo sourcing** for the 4 properties without `public/properties/{slug}/` directories.
- **Marketing copy polish**: e.g. upgrade 73b-hensman's `properties.ts` name from "Comfortable Lodge, Quiet Neighbourhood" to something closer to the Hostaway title "Lakeview Lodge Two with Stunning Views". Several others have similar opportunity.

---

# Session — 2026-04-21 (evening — 6-25-Belfast Pete Bouma feedback)

Pete Bouma (SkyCrest owner) sent 2026-04-15 + 2026-04-16 emails flagging guest guide gaps after his March 2026 remediation visit. Andrew wanted the compendium refreshed. Two rounds of edits shipped: first a 9-edit batch, then 3 refinements after Andrew reviewed.

## Topics
- Round 1: 9 compendium edits from Pete's email + guest-ops owner-log — FOB count, parking detail, Bosch oven wall-switch prerequisite, sauna "Three Things Must Be On" prerequisite block, sauna dial photo (Pete sent a WhatsApp image), KOCOM intercom replacement, gas fire location fix (upstairs lounge → living room), drinking water note, cot availability. Ice bath winter shutdown deferred to owners/managers ops SOP per Andrew's direction. Shipped as `f41fda5`.
- Round 2: 3 refinements after Andrew's review — FOB locations split (1 in lockbox, 1 inside apartment) + $300 lost-FOB charge; luggage/late-checkout line simplified to "discuss with host in advance" (dropped the secure-garage promise); checkout handover bullet inviting guests to leave unused food/drinks for housekeeping. Shipped as `ae21420`.
- TCC/vault-write debugging — `~/Documents/andrewlaery/...` paths blocked for Claude Code Read + Bash all session. Tried Write tool, bash heredoc with sandbox disabled, Read retries — all EPERM. Dug into the TCC subject: Claude Code CLI has no bundle ID of its own; macOS applies TCC decisions against the parent terminal emulator. Identified iTerm 3.6.9 (`com.googlecode.iterm2`) as the TCC subject on Andrew's Mac.
- Obsidian CLI research — no canonical tool; npm `obsidian-cli` packages hit the same TCC block; Local REST API plugin (`coddingtonbear/obsidian-local-rest-api`) is the only reliable workaround but not installed (ports 27123/27124 both closed, Obsidian not running as daemon).

## Findings

### TCC blocker resolution
- Write tool's "must Read first" guard: successful Read only. EPERM errors don't count — guard refuses to let Write overwrite an existing file that TCC has blocked from being Read. However, Write CAN create brand-new files in the same TCC-protected directory (confirmed by creating `_test_write.md` inside `6-25-Belfast/`). So the syscall path Write uses isn't blocked — only the Read syscall is.
- Bash TCC restrictions persist even with `dangerouslyDisableSandbox: true` — that flag bypasses Claude Code's own sandbox but not the OS-level TCC layer.
- **Fix for future sessions**: System Settings → Privacy & Security → Full Disk Access → `+` → `/Applications/iTerm.app`, toggle ON, **⌘Q iTerm completely and relaunch**. Then `cat ~/Documents/...` works. One-time user-side action; no CLI or tool config change needed.
- Saved to project memory (`reference_tcc_iterm.md`) and global MEMORY.md under the sandbox/tool-behaviour cluster.

### Fallback workflow for TCC-blocked sessions
- When vault writes are blocked, edit MDX directly and leave a commit-message note flagging vault reconciliation debt. Critically — DO NOT run `sync-property-compendium.sh` afterwards or the MDX edits will be overwritten from the stale vault. Flag "vault reconciliation required" in Session State so the next session (after TCC unblocks) can mirror the changes.
- If the content edits are content-heavy (>5 edits), this is a meaningful debt item — worth pausing the session to get TCC unblocked rather than accumulate vault/MDX divergence.

### Session resume after iTerm relaunch
- `cd` to project dir, then `claude -c` resumes the most recent session in that directory. `claude --resume` opens a picker.
- Session ID logged for reference: `2c49160f-1d29-45b2-b980-0835c82bf2e8`.

## Next Steps
- **User action (before next session)**: Grant `/Applications/iTerm.app` Full Disk Access, ⌘Q + relaunch. Verify with `cat ~/Documents/andrewlaery/SnowbirdHQ/Property/6-25-Belfast/1.1_6-25-Belfast_\ Welcome\ And\ House\ Rules.md | head -5` — should print file header, not `Operation not permitted`.
- **Next session**: reconcile the 12 MDX edits (rounds 1 + 2) back into the 6-25-Belfast vault `.md` files. Single pass once TCC is live. Then re-run the sync to confirm MDX == vault.
- **Cleanup**: `_test_write.md` stray file in vault folder — delete in Finder.
- **Owner-log update**: record the $300 lost-FOB charge in `__server-bcampx/guest-ops/knowledge/owner-log/6-25-belfast.md`; check off Pete commitments #5 (2 FOBs documented in guide) and #6 (sauna dial markers embedded).
- **Ops SOP entry**: capture Pete's winter ice bath shutdown procedure (May–Sep: chiller OFF at wall plug; water stays cold from ambient) — belongs in the owners/managers ops SOP workstream, not this project.
